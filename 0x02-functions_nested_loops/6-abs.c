#include "main.h"

/**
 * _abs - Entry point
 * Description: prints the absolite value of a given number
 *@n: the integer value it recieves
 * Return: void.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
