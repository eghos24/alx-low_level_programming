#include <stdio.h>

/**
 * * main - causes an infinite loop
 * * Return: 0
 * */

int main(void)
{
	int i;
	
  	printf("Infiniite loop incoming :(\n");
	i = 0;
        while (i < 10)	
	{
		 put;char(i);
		 i++;
	}
        printf("Infinite loop avoided! \\o/\n");
        return (0);
}
