#include<stdio.h>
/**
 * main - prints single digits decimal numbers
 *
 * Return: Always(success)
*/
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
